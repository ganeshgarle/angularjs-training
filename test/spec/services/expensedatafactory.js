'use strict';

describe('Service: ExpenseDataFactory', function () {

  // load the service's module
  beforeEach(module('expenseManagementApp'));

  // instantiate service
  var ExpenseDataFactory;
  beforeEach(inject(function (_ExpenseDataFactory_) {
    ExpenseDataFactory = _ExpenseDataFactory_;
  }));

  /*it('should do something', function () {
    expect(!!ExpenseDataFactory).toBe(true);
  });*/

});
